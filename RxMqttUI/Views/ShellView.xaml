<Page
    x:Class="RxMqttUI.Views.ShellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RxMqttUI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Pivot x:Name="rootPivot" Title="RxMqtt" ItemsSource="{Binding PivotViewItems}" Width="700" HorizontalAlignment="Left">
            <Pivot.RightHeader>
                <CommandBar ClosedDisplayMode="Minimal">
                    <AppBarButton x:Name="Back" Icon="Back" Label="Previous"/>
                    <AppBarButton x:Name="Forward" Icon="Forward" Label="Next" />
                </CommandBar>
            </Pivot.RightHeader>
            <!--<PivotItem Header="Broker">
                --><!--Pivot content goes here--><!--
                <TextBlock Text="Content of pivot item 1."/>
            </PivotItem>
            <PivotItem Header="Client">
                --><!--Pivot content goes here--><!--
                <TextBlock Text="Content of pivot item 2."/>
            </PivotItem>-->
        </Pivot>

    </Grid>
</Page>
